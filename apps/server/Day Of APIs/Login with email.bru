meta {
  name: Login with email
  type: http
  seq: 2
}

post {
  url: {{Server}}/api/employees/login
  body: json
  auth: inherit
}

body:json {
  {
    "email": "john.dept@example.com",
    "password": "wnEvGLj6DQH*"
  }
}

settings {
  encodeUrl: true
  timeout: 0
}
