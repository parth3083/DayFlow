meta {
  name: Login
  type: http
  seq: 2
}

post {
  url: {{Server}}/api/employees/login
  body: json
  auth: inherit
}

body:json {
  {"email": "admin@example.com", "password": "password123"}
}

settings {
  encodeUrl: true
  timeout: 0
}
